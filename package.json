{
   "name": "phase-4-deploying-app-demo",
   "description": "Build scripts for Heroku",
   "engines": {
      "node": "16.x"
   },
   "scripts": {
      "prebuild": "echo 'export PATH=\"/usr/local/opt/libpq/bin\"' && gem install pg"
   },
   "files.associations": {
      "*.html.erb": "erb"
   }
}
